{
  "schema_version": 1,
  "backlog_mode": "local",
  "spec_root": "specs",
  "commands": {
    "test": "npm test"
  },
  "workflow_defaults": {
    "work_mode": "atom",
    "commit_granularity": "per-increment",
    "pr_policy": "auto-per-increment",
    "validation_mode": "blocking",
    "spec_sync_mode": "proactive"
  }
}
